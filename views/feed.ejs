<!DOCTYPE html>
<html>
<head>
  <title>Feed</title>
  <style>
    /* Add styles to make the page more presentable */
    body {
      background-color: #f0f0f0;
      font-family: Arial, sans-serif;
      position: relative;
    }
    .feed {
      background-color: #fff;
      border: 1px solid #ddd;
      border-radius: 5px;
      padding: 20px;
      margin: 20px;
      width: 95%;
      height: 1200px;
    }
    h2 {
      font-size: 18px;
      margin-bottom: 10px;
    }
    p {
      margin-bottom: 10px;
    }
    small {
      color: #aaa;
    }
    .feed-image {
      max-width: 100%;
      max-height: 100%;
      display: table-column;
    }
    .profile-image {
      position: absolute;
      top: 10px;
      right: 10px;
      width: 50px;
      height: 50px;
      border-radius: 50%;
    }
  </style>
</head>
<body>
  <h1>Feed</h1>

  <form action="/upload" method="POST" enctype="multipart/form-data">
    <input type="file" name="image" />
    <button type="submit">Upload</button>
</form>

<div class="feed">
  <a href="/user/<%= brukernavn %>"><img class="profile-image" src="./images/profile.png"></a>
  <% uploads.forEach((item) => { %>
    <div>
      <img class="feed-image" src="./uploads/<%= item.bildenavn %>">
      <p>Uploaded by: <%= item.eier_bilde %></p>
    </div>
  <% }); %>
</div>

</body>
</html>
